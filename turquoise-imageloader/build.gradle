
dependencies {
    //required
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile project(':turquoise') { group 'sviolet'; version upload_mvn_version }//指定groupId和version, artifactId为project名
}